# Short Response Question
1. If I am collaborating with a classmate, the first thing I have to do is to create a pull request. This step is important so that your classmate can be able to merge both files together.
2. Developers use branches to work on a project without being able to mess up the main.
3. Git is a command line tool that allows you to track and manage changes to your code. Git is also a tool that allows us to interact with other repositories. Github is a web based platform to store repos in the cloud and collaborate with other developers. Git and Github work together because we can use git in github through the GUI to store information.
4. What is wrong with the format is that git add . always comes first because the command with take a snapshot of the files at that moment and it is always the first step to save your work. the correct way to format it would be:
git add . 
git commit -m "saving work" 
git push